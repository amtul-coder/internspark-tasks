const API="https://openlibrary.org/search.json";
const results=document.getElementById("results"),state=document.getElementById("state"),count=document.getElementById("count"),title=document.getElementById("resultsTitle"),input=document.getElementById("searchInput");
function loading(){state.className="state";state.textContent="Loading books...";results.innerHTML="";count.textContent=""}
function error(msg){state.className="state error";state.textContent=msg;results.innerHTML="";count.textContent=""}
function render(books){results.innerHTML="";if(!books.length){state.textContent="No books found. Try another search.";return}state.textContent="";count.textContent=`${books.length} results`;books.slice(0,20).forEach(b=>{const card=document.createElement("article");card.className="card";const img=document.createElement("img");img.className="cover";img.alt=`${b.title||"Book"} cover`;img.loading="lazy";if(b.cover_i)img.src=`https://covers.openlibrary.org/b/id/${b.cover_i}-M.jpg`;else img.style.display="none";img.onerror=()=>img.style.display="none";const body=document.createElement("div");body.className="card-body";const h=document.createElement("h3");h.className="title";h.textContent=b.title||"Untitled";const a=document.createElement("p");a.className="author";a.textContent=(b.author_name||["Unknown author"]).slice(0,2).join(", ");const y=document.createElement("div");y.className="year";y.textContent=b.first_publish_year?`First published ${b.first_publish_year}`:"Publication year unavailable";body.append(h,a,y);card.append(img,body);results.append(card)})}
async function searchBooks(q){q=q.trim();if(!q){error("Please enter a book title, author, or topic.");return}loading();title.textContent=`Results for “${q}”`;try{const r=await fetch(`${API}?q=${encodeURIComponent(q)}&limit=20`);if(!r.ok)throw new Error();const data=await r.json();render(data.docs||[])}catch(e){error("Unable to load books right now. Please check your connection and try again.")}}
document.getElementById("searchForm").addEventListener("submit",e=>{e.preventDefault();searchBooks(input.value)});
document.querySelectorAll(".quick").forEach(b=>b.addEventListener("click",()=>{input.value=b.dataset.query;searchBooks(b.dataset.query)}));
searchBooks("bestsellers");